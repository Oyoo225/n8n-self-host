# docker-compose.yml
# This file is typically for local development and is NOT directly used by Render
# for deploying a single Web Service. You can keep it for local testing if needed,
# but it won't affect your Render deployment based on the render.yaml.

version: '3'
# services:
#   n8n:
#     image: n8nio/n8n
#     ports:
#       - "5678:5678"
#     volumes:
#       - ./workflows:/home/node/.n8n/workflows
#     environment:
#       - GENERIC_TIMEZONE=Asia/Bangkok
#       - N8N_HOST=localhost
#       - N8N_PORT=5678
#       - N8N_PROTOCOL=http
#       - N8N_BASIC_AUTH_ACTIVE=true
#       - N8N_BASIC_AUTH_USER=your_local_username
#       - N8N_BASIC_AUTH_PASSWORD=your_local_password
