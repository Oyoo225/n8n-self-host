GENERIC_TIMEZONE=Asia/Bangkok
N8N_HOST=n8n-self-host-0xgw.onrender.com # Use the exact hostname Render provides you
N8N_PORT=5678
N8N_PROTOCOL=https

# IMPORTANT: Set these for security if you expose the n8n UI
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=kyawthiha # REPLACE WITH A STRONG USERNAME
N8N_BASIC_AUTH_PASSWORD=12345 # REPLACE WITH A STRONG PASSWORD

# If your Google Gemini API key needs to be passed via n8n credentials,
# you will need to map it in n8n itself.
# For example, if you use an environment variable for your API key in n8n's credentials setup:
# GOOGLE_GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
